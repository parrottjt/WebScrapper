<Window x:Class="WebScrapper.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WebScrapper.App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10px">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MaxWidth="10px"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" MaxWidth="10px"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="10px"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50px"/>
            <RowDefinition Height="Auto" MinHeight="10px"/>
        </Grid.RowDefinitions>

        <TextBox Name="HtmlString" Grid.Row="1" Grid.Column="1"  Grid.ColumnSpan="3" VerticalScrollBarVisibility="Auto"/>
        <Button Name="Generate" Grid.Row="2" Grid.Column="2" Margin="10" Click="WebPageDataClick">
            Generate Data
        </Button>
    </Grid>
</Window>
